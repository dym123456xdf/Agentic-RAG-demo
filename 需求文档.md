## 一、产品定位

一个端到端的 **模块化检索增强生成(RAG)** 流水线。把"让 LLM 基于你自己的资料回答问题"这件事,拆成 5 个独立、可替换的环节,每一段都有自己的职责,合起来就是一个完整的智能问答系统。

核心价值:
- **可控**:每一段都可以单独替换/调参(换 LLM、换 embedding、换重排模型、换检索策略,不影响其他段)
- **可观测**:每一段都有日志输出,定位问题很直接
- **高性能**:在通用 RAG 流程上叠加了查询预处理(意图/改写/扩展)和重排序,显著提升召回质量

## 二、技术栈

| 层 | 选用技术 | 在本系统中的角色 |
|---|---|---|
| LLM | MiniMax-M3(minimax 自研模型,OpenAI 兼容协议) | 用于意图识别、查询改写、查询扩展、最终答案生成 |
| Embedding | embo-01(minimax 向量模型,维度 1536) | 文档入库和检索 query 都用它向量化 |
| 向量数据库 | Milvus(standalone 部署) | 持久化存储文档向量,提供相似度检索 |
| 文档加载 | UnstructuredReader(llama-index 内置) | 处理 PDF / DOCX 等非 Markdown 文档 |
| 文档切分 | MarkdownNodeParser(llama-index 内置) | 按 Markdown 标题层级切分段落 |
| 索引框架 | llama-index `VectorStoreIndex` | 提供向量索引抽象 + Retriever 接口 |
| 检索策略 | `VectorIndexRetriever` + `QueryFusionRetriever`(llama-index 内置) | 纯向量检索,以及单 retriever × 多次 query 扩展的混合检索 |
| 重排序 | BAAI/bge-reranker-v2-m3(sentence-transformers CrossEncoder) | 在 Apple Silicon MPS 上跑,对召回结果重新打分 |
| 配置管理 | python-dotenv | 从 `.env` 文件加载 API key / 服务地址等敏感配置 |
| 运行时 | Python ≥ 3.11,macOS 支持 MPS GPU 加速 | 跨平台,无 MPS 时 fallback 到 CPU |

## 三、整体架构

系统由 5 个模块组成,按流水线顺序工作:

```
用户问题 → 查询预处理 → 文档召回 → 后处理(过滤+重排) → 答案生成 → 返回答案+来源
```

## 三、各模块功能详解

### 模块 1:配置中心

统一管理所有外部依赖的连接信息(API key、服务地址、模型名等),从环境变量读取,所有模块共享同一份配置。

- LLM API key / 服务地址 / 模型名
- Embedding API key / GroupId / 模型名(embedding 必填 GroupId,用于租户隔离和计费归属)
- 向量库连接地址 + collection 名 + 向量维度
- 重排序保留条数

### 模块 2:索引构建

负责把原始资料变成可检索的向量索引。提供两种模式:

**首次构建模式**(建索引一次):
1. 加载文档(支持 Markdown / PDF / DOCX 等;)
2. 按 Markdown 标题层级切分成段落(保留语义边界)
3. 把每个段落向量化后写入向量库
4. 构建可查询的索引对象

**复用索引模式**(后续每次启动):
- 直接从已有 collection 加载,跳过重新加载文档、跳过重新 embedding
- 避免重复 token 消耗和时间浪费

### 模块 3:查询预处理

在检索之前,对用户原始问题做"加工",提升后续召回质量。包含三步:

1. **意图识别**:把问题分类为事实型 / 解释型 / 比较型 / 创造性 / 闲聊型,为后续可能的不同处理策略留口子
2. **多轮改写**:结合对话历史,把指代词、省略的主语补全,让问题更完整更明确(无历史则原样跳过)
3. **查询扩展**:生成 3 个语义相关的扩展问题,用于提升检索覆盖面

输出为一个结构化对象,包含原 query、改写后 query、扩展 query 列表、意图标签。

### 模块 4:文档召回

基于预处理后的 query 去向量库捞相关片段。提供两种策略:

- **混合检索(默认)**:对单条 query 自动扩展多个变体分别召回,然后融合去重排序,召回覆盖面更广
- **纯向量检索**:直接做单次向量相似度召回,速度更快

召回数量可在配置中调,默认 10 条。

### 模块 5:检索后处理

召回的 10 条原始结果良莠不齐,这一段做两件事把它们"精修":

1. **相似度阈值过滤**:基于实测向量距离范围,把明显不相关的片段砍掉,减少重排输入量
2. **重排序**:用专业的 cross-encoder 模型对剩余片段重新打分排序,只保留最相关的几条(默认 5 条)送入生成阶段

为什么用专门的本地重排模型,不用 LLM 重排:
- 速度:快约 30 倍
- 成本:便宜 10-20 倍
- 精度:对中英双语的 query-doc 相关性判断更准

代价是首次加载该模型需要几秒到十几秒(模型约 2.2GB),加载一次后复用。

### 模块 6:答案生成

把重排后的高质量片段拼成上下文,连同用户问题一起交给 LLM,生成最终回答。

输出包含两部分:
- **答案文本**:基于上下文生成的回答(明确告知"上下文中没有就说没有",不编造)
- **来源列表**:每条来源包含编号、内容预览、相关度分数、原始元数据(如来源文件名)

可选支持流式输出。

## 四、关键设计取舍

**为什么 embedding 必须自写适配器,而不能直接用通用 OpenAI 风格封装?**

本系统选用的 embedding 服务,虽然端点路径也叫 `/v1/embeddings`,但跟 OpenAI 的同名协议 **不兼容**,有三处硬差异:
- 请求体字段名不同
- 租户标识必须通过 URL query string 传,不能放在 body
- 响应结构不同(返回 `vectors` 字段,不是 `data[].embedding`)

直接用通用封装会出现:请求 400 invalid params、鉴权 401、维度不匹配导致向量库写入失败、检索永远召回 0 条等连锁问题。

**为什么入库和检索要区分编码模式?**

该 embedding 服务把"被检索的文档"和"查询语句"用两套不同的编码空间(分别叫 `db` 模式和 `query` 模式)。两边编码空间不一样,**混用会导致召回率显著下降**(虽然能跑通,但结果很差)。

系统封装了这两层语义,上游调用方无需关心。

**为什么用 Markdown 标题切分,不用固定长度切分?**

固定长度切分会从句子中间切断,丢失语义。Markdown 文档天然有标题层级结构,按标题切分能保证每个片段是一个语义完整的段落,检索时语义匹配更准。

**为什么对话历史只取最近 3 条?**

历史越长,改写 prompt 越长,token 消耗越多,延迟也越高。实测最近 3 轮足够覆盖上下文指代,再多收益递减。

**为什么重排的相似度阈值定在 0.2?**

基于实测该向量库的召回距离分布:大多数有效片段的距离在 0.26~0.53 之间。阈值设到 0.7 会把全部片段砍光,设到 0.2 是合理下限 —— 低于 0.2 的基本真的不相关。真正的精排交给 cross-encoder。

## 五、典型使用场景

- **企业知识库问答**:把内部文档(产品手册、规章制度、技术文档)导入后,员工可以用自然语言提问
- **技术文档助手**:把项目文档、API 文档构建成索引,开发者可以直接问"怎么配置 XX"
- **客服/FAQ 智能回复**:把历史问答、帮助中心文档接入,自动回答用户常见问题
- **个人资料库**:把笔记、收藏的文章构建成索引,作为个人第二大脑

## 六、输入输出规格

**输入**:
- 资料侧:文档文件或目录(支持 Markdown、PDF、DOCX 等)
- 用户侧:自然语言问题 + 可选的对话历史

**输出**:
- 自然语言回答
- 参考来源列表(编号 / 内容预览 / 相关度分数 / 来源文件)
